using RIAT.serializer;
using RIAT.unit;

namespace serializer
{
    class Program
    {
        static void Main(string[] args)
        {
            //Input input = Input.CreteRandom();
            //Output output = Output.createFrom(input);

            //Console.WriteLine($"Несериализованный объект {input}");

            //var serialization = new Serializer();
            //var inputDiskSerializedJOSN = serialization.serializationJosn(output);
            //var inputDiskSerializedXml = serialization.serializationXml(output);

            //Console.WriteLine($"Cериализованный объект {inputDiskSerializedJOSN}");
            //Console.WriteLine($"Cериализованный объект XML {inputDiskSerializedXml}");

            //var outputJosn = serialization.deserializeJson<Output>(inputDiskSerializedJOSN);
            //var outputXml = serialization.deserializeXml<Output>(inputDiskSerializedXml);

            //outputJosn.Write();
            //outputXml.Write();
        }
    }
}

